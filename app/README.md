# App 前端

这里将创建全新的 uni-app + Vue 3 + TypeScript 前端。

不要从 `E:\小程序开发\前端code\elder_care` 复制目录、页面、组件、样式或假数据。需要参考原项目内容时，只提取经过确认的业务概念，并按 `design/` 中的新设计重新实现。

建议通过 `D:\hbuilder x\HBuilderX\HBuilderX.exe` 创建/打开本目录下的 App 工程。第一批页面实现前，先完成设计确认和 OpenSpec change。
