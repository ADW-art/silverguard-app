# SilverGuard 视觉系统

## 方向

视觉方向为“暖金守护”。老人端保留适老服务产品熟悉的大入口结构，以香槟金、奶油白、深墨色构成温暖可靠的主界面；家属端使用统一的中明度雾松绿画布、乳白内容表面和克制的松针绿，形成清透、从容且边界清楚的守护氛围。高级感来自稳定画布、表面色阶、留白和字体层级，不依赖分段背景或装饰性柔光。

家属端采用 UI UX Pro Max 的 `Material 3 tonal surface` 色调层级，并参考平台材料规范把半透明效果限制在导航与关键功能层；`Neumorphism`、整页玻璃化和过浅医疗蓝绿方案均因层级或对比度风险而弃用。

## 核心 Tokens

| 用途 | 值 |
| --- | --- |
| Elder / Primary | `#D6A84B` |
| Elder / Deep | `#9C6919` |
| Elder / Background | `#F8F2E6` |
| Family / Primary | `#22594D` |
| Family / Deep | `#133B32` |
| Family / Background | `#C8DCD2` |
| Family / Surface | `#F7FAF8` |
| Family / Surface Low | `#E8F0EC` |
| Family / Text Secondary | `#4F5A54` |
| Text / Primary | `#231E17` |
| Text / Secondary | `#6E665A` |
| Surface | `#FFFDF8` |
| Risk / High | `#B84438` |
| Spacing | `4 / 8 / 12 / 16 / 24 / 32` |
| Radius | `8 / 12 / 16 / 20 / 24` |

## 材质

- 老人端玻璃表面：白色 70–86% 不透明度、背景模糊 12–18px、1px 白色高光边。
- 家属端统一画布：固定使用中明度雾松绿 `#C8DCD2`，整屏不切换底色，不添加顶部渐变带或大型装饰光斑。
- 家属端内容表面：常规卡片使用 `#F7FAF8`，主状态使用 `#E8F0EC`，高风险摘要使用 `#F9EAE5`；通过稳定的色调差建立边界。
- 家属端玻璃表面：仅用于守护岛、底部导航和固定操作条，白色 82–90% 不透明度、约 16px 背景模糊、1px 高明度描边。
- 环境阴影：老人端使用黑色 8–12% 不透明度；家属端普通内容使用松针绿约 3.5%，功能浮层使用 6%，Y 轴 4–6px，模糊 14–18px。
- 渐变：老人端用于暖金主视觉；家属端只用于主要操作按钮，不用于页面背景和内容分区。
- 珍珠釉实体：顶部白色内高光、底部深色柔和内阴影和大半径环境投影共同建立触感；用于主视觉和高优先入口。
- 清透雾面：高不透明半透明表面配细白描边和轻微背景模糊；只用于家属端守护岛、导航和固定操作层，不承载普通内容卡片。
- 微颗粒与同心光环：透明度保持在背景级，只用于消除大面积纯色的塑料感，不承载信息。
- 页面材质层级限制为釉面实体、清透雾面和玻璃导航三类，避免效果堆叠。

## 内容密度与产品语言

- 一个功能入口默认只由一个图标和一个动作名称组成，不追加同义说明。
- 行为约束并入动作名称，例如“长按求助”，不再拆成“紧急求助 / 长按 3 秒”。
- 每屏只突出一个主状态与一组主操作；不为填满空间增加卡片、标签或状态句。
- 只有会改变用户决定的状态、时间和来源才保留第二层文字。
- 核心任务页面不使用 AI 徽章、机器人、星光符号或“AI 为你……”式文案；必要时使用“状态摘要”“数据提示”等自然语言。

## 适老与告警边界

- 老人端主要正文至少 18px，辅助标签可使用 14–16px；Android 点击区域至少 48dp。
- 正文对比度至少 4.5:1；玻璃表面上的关键文字需按最终合成背景检查。
- 状态必须同时使用文字或图标，不能只依赖颜色。
- 危险操作、告警正文、确认弹窗使用实体表面，不在低透明玻璃上承载长文本。
- 动效只使用 150–300ms 的透明度、阴影和轻微位移变化，并支持减少动态效果。

## 页面策略

- 老人端首页：暖金状态主视觉、两项高优先入口、两项辅助入口、用药提醒、语音操作、玻璃底部导航。
- 老人端用药提醒：大时间和药品主视觉、单一确认服药动作、三行今日计划、联系家人入口。
- 家属端首页：顶部守护岛、单一整体状态、抽象守护圆环、联系老人/今日记录、一张活动趋势图和底部导航；设备与告警列表不在首页平铺。
- 家属端告警详情：告警结论、一句状态摘要、单一趋势图、处理时间线和随接管状态变化的固定操作条。
- 家属端守护页：按生活空间组织设备，使用生活化名称；技术编号、协议和后台配置不进入主界面。

## 禁止项

- 通用 AI 插画、卡通老人和无信息价值的装饰图。
- 紫蓝 AI 渐变、彩虹图标、霓虹外发光。
- 全页面玻璃化、低对比灰字、所有卡片使用同一强阴影。
- 关键按钮小于 48dp、只用颜色表达风险、把 AI 建议写成医疗诊断。
- 按钮下方重复解释动作、模板化三层文案、无决策价值的小字和装饰标签。
- 真实室内照片、人物照片、监控画面和摄影背景；图表与状态层必须建立在纯色或抽象柔光表面上。
- 页面背景分段、顶部渐变色带、大面积装饰光斑、所有卡片同时玻璃化，或任何影响正文对比度的纹理。
